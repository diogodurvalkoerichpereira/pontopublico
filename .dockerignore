# Nunca enviar para o contexto de build: node_modules do host tem binários de
# outra plataforma e .output seria sobrescrito pelo build de dentro da imagem.
node_modules
.output
dist
.vite

# Segredos e ambiente local.
.env
.env.*
!.env.example

# Ruído de repositório.
.git
.gitignore
.claude
*.log
npm-debug.log*

# Documentação: não participa do build.
*.md
docs/
